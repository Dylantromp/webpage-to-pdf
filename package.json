{
  "name": "webpage-to-pdf",
  "version": "1.0.0",
  "description": "API to convert webpages to PDF",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.9.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=14.x"
  }
}
