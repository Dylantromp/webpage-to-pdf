{
  "version": 2,
  "builds": [{
    "src": "api/convert.js",
    "use": "@vercel/node",
    "config": { 
      "maxDuration": 30,
      "suppressBuildLogs": true 
    }
  }],
  "routes": [{
    "src": "/api/(.*)",
    "dest": "/api/convert"
  }]
}
